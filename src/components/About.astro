<section class="py-20">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-2xl font-bold text-text-heading font-mono mb-2">
      <span class="text-accent-purple">~/</span>about
    </h2>
    <div class="h-px bg-gradient-to-r from-accent-purple/50 to-transparent mb-8"></div>

    <div class="bg-bg-elevated border border-white/5 rounded-lg p-6 sm:p-8 space-y-4 text-text-primary leading-relaxed">
      <p>
        I'm a Software Engineer who landed in Performance Engineering — and never looked back.
        From running <span class="text-accent-purple font-medium">k6 load tests at 100k RPS</span> to optimizing
        <span class="text-accent-purple font-medium">Kubernetes</span> clusters on AWS, I spend my days making
        systems faster and proving they hold up before real traffic ever finds the cracks. Today I lead
        performance engineering at <span class="text-accent-purple font-medium">EPAM Systems</span>.
      </p>
      <p>
        Nights and weekends, I ship products — mostly for the Mexican market. I built
        <span class="text-accent-purple font-medium">Vitrina</span>, a WhatsApp-native site generator running on a
        zero-dependency Node backend, and <span class="text-accent-purple font-medium">MX Stock Analyzer</span>, an
        AI-powered analysis SaaS for the BMV. The same instinct that tunes a slow query pushes me to turn
        rough ideas into things people can actually use.
      </p>
      <p>
        Lately that means going deep on AI: designing <span class="text-accent-purple font-medium">LLM agent systems</span>
        like AgentOS, wiring Claude and friends into real pipelines, and open-sourcing the tooling I wish existed —
        like <span class="text-accent-purple font-medium">k6gen</span>, which generates load tests from an OpenAPI
        spec with an LLM.
      </p>
      <p>
        Whether I'm profiling a bottleneck, assembling an agent pipeline, or teaching someone how to debug a
        race condition — I'm most at home when the problem is hard and the solution has to be precise.
      </p>
    </div>
  </div>
</section>
